# Configuration

Environment variables:

- `LINEAR_API_KEY` - Linear token
- `GITHUB_TOKEN` - GitHub token for PR automation (optional)
- `DEFAULT_MODEL` - optional default model alias

See `.env.example`.
